pipeline{
    agent any
    
    environment{
        PATH = "/opt/maven3/bin:$PATH"
    }
    stages{
        stage("Git Checkout"){
            steps{
                git branch: 'sai', credentialsId: 'sharath', url: 'https://github.com/SAISHARMA9804/myweb.git'
            }
        }
        stage("Maven Build"){
            steps{
                sh "mvn clean package"
                sh "mv target/*.war target/myweb.war"
            }
        }
        stage("deploy-dev"){
            steps{
               sshagent(credentials: [''], ignoreMissing: true) {
                sh """
                    scp -o StrictHostKeyChecking=yes target/myweb.war  root@tomcat-instance:/home/ec2-user/apache-tomcat-9.0.73/webapps/
                    
                    ssh root@tomcat-instance /home/ec2-user/apache-tomcat-9.0.73/bin/shutdown.sh
                    
                    ssh root@tomcat-instance /home/ec2-user/apache-tomcat-9.0.73/bin/startup.sh
                
                """
            }
            
            }
        }
    }
}
